import styled from 'styled-components'

export const IndexWrapper = styled.div`
  position: relative;
  #card {
    max-width: 80%;
    margin: 10vh auto;
    button {
      margin: 4rem 0;
    }
  }
`
