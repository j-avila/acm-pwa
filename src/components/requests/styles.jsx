import styled from 'styled-components'

export const Wrapper = styled.div`
  position: relative;
  min-height: 500px;
  .btn-new {
    position: fixed;
    bottom: 10vh;
    left: 45%;
    transform: translateX(-45%);
  }
`
