import styled from 'styled-components'

export const Wrapper = styled.div`
  background: white;
  border-radius: 5px;
  padding: 27px;
  box-shadow: ${({ theme }) => theme.shadow};
  display: block;
  width: 95%;
`
